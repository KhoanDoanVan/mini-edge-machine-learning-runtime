# Mini Edge Runtime

A from-scratch learning implementation of an ONNX-style model format and inference runtime for mobile, edge, and on-device AI. It does not depend on ONNX, ONNX Runtime, NumPy, or an ML framework.
